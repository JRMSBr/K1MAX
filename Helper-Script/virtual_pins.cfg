[gcode_macro TESTTTTT]
description: W
gcode:
    {% if printer['output_pin power'].value == 0 and (not printer['bed_mesh'].profile_name) %}
       RESPOND TYPE=command MSG="PIN desligado"
      {% endif %}

      {% if printer['output_pin power'].value == 0 and (not printer['bed_mesh'].profile_name) %}
       RESPOND TYPE=command MSG="Pin ligado"
      {% endif %}